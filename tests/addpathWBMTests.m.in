% Add the compiled mex in the build directory 
addpath('${CMAKE_CURRENT_BINARY_DIR}/..');

% Add the matlab files in the source directory
addpath(genpath('${CMAKE_CURRENT_SOURCE_DIR}/../matlab-src'));