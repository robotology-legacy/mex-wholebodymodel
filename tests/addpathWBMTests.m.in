% Add the compiled mex in the build directory
addpath(genpath('${CMAKE_CURRENT_BINARY_DIR}/../mex-wholebodymodel/library'));

% Add the matlab interface files in the source directory
addpath('${CMAKE_CURRENT_SOURCE_DIR}/../mex-wholebodymodel/matlab/wrappers');

% Add the helpers functions
addpath('${CMAKE_CURRENT_SOURCE_DIR}/../mex-wholebodymodel/matlab/utilities');

% Add the test script in the source directory
addpath('${CMAKE_CURRENT_SOURCE_DIR}')

% Add the models directory
addpath('${CMAKE_CURRENT_SOURCE_DIR}/models')